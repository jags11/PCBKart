<nav class="navbar navbar-expand-lg navbar-light ">
  <%= link_to 'PCB Kart',  root_path, class: "navbar-brand", id: "logo" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <% if logged_in? && current_user.is_admin == false %>
          <%= link_to 'My Orders', orders_path, class: "nav-link" %>
        <% else %>
          <% if logged_in? %>
            <%= link_to 'All Orders', orders_path, class: "nav-link" %>
          <% end %>
      <% end %>
      </li>

      <% if logged_in? && current_user.is_admin == true %>
        <li class="nav-item active">
          <%= link_to 'Layers', layers_path, class: "nav-link" %>
        </li>
        <li class="nav-item active">
          <%= link_to 'Layer Thickness', layerthicknesses_path, class: "nav-link" %>
        </li>
        <li class="nav-item active">
          <%= link_to 'PCB Pricing', pcb_pricings_path, class: "nav-link" %>
        </li>
      <% end %>

      <% if logged_in? %>
        <li class="nav-item active">
          <%= link_to 'Log out', logout_path,  method: "delete", class: "nav-link" %>
        </li>
        <li class="nav-item active">
          <div class="nav-link">
            <%= current_user.email %>
          </div>
        </li>
      <% else %>
        <li class="nav-item active">
          <%= link_to 'Login', login_path, class: "nav-link" %>
        </li>
      <% end %>
        </ul>
  </div>
</nav>